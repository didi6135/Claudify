# Task 3.6 — Security hardening pass + README Security section

**Status:** ⏳ pending
**Estimated effort:** ~1-2 hrs
**Depends on:** 3.5 (everything in scope must already be implemented)
**Blocks:** Phase 3 closure

## Goal

Verify that the security model documented in `architecture.md §11` is
*actually true in code*. Audit every claim, fix every gap, document the
result. Then write a user-facing Security section in README so users
can read the threat model in plain language.

## Why

`CLAUDE.md` rule 6: *"Security-by-default per change."* Each prior task
should have respected this, but no one has done a comprehensive sweep
across the whole codebase. This task is that sweep. After Phase 3
closes, no one is going to come back to do this — so it has to land
now.

## Scope

### Files to audit (read-only, mostly)

Every shell file under `lib/`, `lib/engines/`, every TS file under
`src/`, every entrypoint at the repo root, every systemd unit
template, every example in README and `architecture.md`.

### Files likely to modify

- `lib/configs.sh` — chmod calls; file write atomicity
- `lib/oauth.sh` — token redaction in logs; no secrets in argv
- `lib/service.sh` — verify `EnvironmentFile=` is used (not `Environment=`) for any secret
- `lib/manifest.sh` — atomic write discipline confirmed
- `src/lib/log.ts` — TS-side redaction matches bash side
- `src/backup.ts` / `restore.ts` — tar perms preserved; no logged secrets
- `README.md` — new "Security" section; link to architecture.md §11
- `docs/architecture.md` §11 — flip status markers ("now verified") + add audit summary
- `CHANGELOG.md` — `### Security` entry summarizing the audit

### Files to create

- `tests/bash/security.bats` — assertions for the audit findings (chmod values, no-eval, secret-not-in-ps, etc.)

## The audit checklist

Walk through each line; mark ✅ verified or 🔴 fix needed.

### 1. Secrets at rest

- [ ] Every file under `~/.claudify/instances/<name>/` that contains a secret is chmod 600
- [ ] `~/.claudify/instances/<name>/credentials.env` 600
- [ ] `~/.claudify/instances/<name>/channels/*/.env` 600
- [ ] `~/.claudify/instances/<name>/mcps/*/oauth.json` 600
- [ ] `access.json` is 644 (NOT secret — IDs only)
- [ ] systemd unit file 644 (NOT secret — references EnvironmentFile)
- [ ] `~/.local/bin/<name>` wrapper 755 (executable, no secrets inside)

### 2. Secrets in transit / process surface

- [ ] No `Environment=` line with a secret in any systemd unit (must use `EnvironmentFile=`)
- [ ] No `claude` / `bun` / npm / curl call where a secret is passed as a positional argument
- [ ] No `echo` / `printf` of a secret to stdout/stderr without redaction
- [ ] sed-redact pattern applied to every install/update/doctor log file before user-facing display
- [ ] `claudify-install-*.log` files don't contain raw secrets (sed in `setup_logging` redacts)

### 3. Input validation

- [ ] `BOT_TOKEN` regex enforced at every entry point (env + prompt)
- [ ] `TG_USER_ID` regex enforced
- [ ] Instance name regex + blocklist enforced
- [ ] No `eval` on operator input anywhere — `grep -rn 'eval' lib/ src/ install.sh update.sh uninstall.sh doctor.sh backup.sh restore.sh build.sh test.sh`
- [ ] Every `$var` expansion in bash is double-quoted

### 4. Path traversal / symlink attacks

- [ ] All paths are absolute (`$HOME` / `$CLAUDIFY_ROOT`-anchored)
- [ ] `mv`, `rm -rf`, `tar -x` operate only inside `~/.claudify/`, `~/.config/systemd/user/`, or explicit user-supplied dirs
- [ ] `restore.sh` validates the tarball doesn't contain `..` segments before extracting

### 5. Privilege

- [ ] No `sudo` calls during normal operation (install, update, uninstall, doctor, backup, restore)
- [ ] `sudo` only at install for `loginctl enable-linger` — interactive, not automated
- [ ] No `setuid` or `setgid` shenanigans
- [ ] Service unit doesn't grant capabilities; runs as the user

### 6. Network

- [ ] All curl calls use `https://`
- [ ] No `--insecure` flag
- [ ] No `-k` flag
- [ ] We don't override `CURL_CA_BUNDLE` or similar
- [ ] No outbound to non-HTTPS endpoints anywhere in the stack

### 7. Supply chain

- [ ] `dist/install.sh` committed and identical to a fresh `bash build.sh` output (run it; check `git diff`)
- [ ] Bun installed from the official one-liner only (`https://bun.sh/install`)
- [ ] Claude Code installed from `@anthropic-ai/claude-code` only
- [ ] No vendored binaries / blobs in the repo

### 8. Multi-instance isolation

- [ ] Two instances don't read each other's secrets even when same user (file perms)
- [ ] Two instances' `bun` subprocesses don't share TELEGRAM_STATE_DIR
- [ ] Test: install instance `alpha` and `beta`; run `lsof | grep claudify` — should see both unit's processes operating only inside their own paths

### 9. Error handling

- [ ] Every `|| true` in shell has a comment explaining why
- [ ] Every `try/catch` in TS has a meaningful error message
- [ ] No silent swallowing of failures from `chmod`, `mv`, `mkdir -p` (they normally don't fail, but if they do we want to know)

## Steps

1. Walk the checklist systematically; fix each ❌ as found
2. Add `tests/bash/security.bats` covering the file-permission assertions and the no-eval grep
3. Write the **README.md "Security" section** as a user-facing summary:
   - 1 paragraph: what's protected
   - 1 paragraph: what's NOT protected (link to architecture.md §11h for full list)
   - 1 paragraph: what to do as an operator (the action items from `architecture.md §11i`)
4. Update `architecture.md §11` — flip "Phase 5 will add" markers where 3.6 actually closed gaps; leave the truly-deferred items (cost ceiling, signed releases) as-is
5. CHANGELOG `### Security` entry: bullet list of what was audited and what was tightened
6. Final Station11 sanity:
   - `chmod 600` files are 600
   - `ps aux | grep claudify` doesn't show secrets
   - `default doctor` 28/28 green

## Acceptance criteria

- [ ] Every checklist item ✅ or has an explicit "deferred to Phase 5" with reason
- [ ] `tests/bash/security.bats` covers at least 5 audit items and passes
- [ ] README has a Security section accessible from the TOC
- [ ] architecture.md §11 status updated
- [ ] CHANGELOG `### Security` entry in Unreleased
- [ ] Station11 file perms verified
- [ ] No `Environment=<secret-key>=` anywhere in any unit file
- [ ] No `eval` matches in source

## Test plan

- **Local:** `grep -rn 'eval' lib/ src/ *.sh` — should be empty (or each match annotated as safe)
- **Local:** bats security tests pass
- **Station11:** `find ~/.claudify -type f -exec stat -c '%a %n' {} \; | grep -v '^[67]00\|^644'` — should print nothing surprising
- **Station11:** `systemctl --user cat claudify-default | grep -E '^Environment='` — only non-secret env vars
- **Station11:** `journalctl --user -u claudify-default | grep -oE 'sk-ant-oat01-[A-Za-z0-9_-]+' | head` — should be empty (token never logged plaintext)

## Out of scope

- Cost ceiling enforcement (Phase 5)
- Audit log of bot actions (Phase 5)
- Signed releases / commits (Phase 5)
- Encrypted-at-rest secrets via age/sops (Phase 5)
- Penetration testing by an external party (post-Phase 5)

## Notes / risks

- The audit will turn up *something*. Plan a buffer — if you find a real issue mid-audit, fix it before continuing the pass; don't accumulate findings.
- README's Security section is user-facing — keep it readable, not a wall of text. The detailed threat model lives in architecture.md §11; README's job is to point users there.
- Some checklist items might require running on Station11 (e.g., file-perm `find`). Plan a 15-minute Station11 session as part of this task.
